import {
  defaultLanguage,
  supportedLanguages,
  defaultLanguageObject,
  supportedLanguagesObjects,
} from '../../next-i18next.config';

export const languageCookieName = 'NEXT_LOCALE';

export {defaultLanguage, supportedLanguages, defaultLanguageObject, supportedLanguagesObjects};
